const num1 = 31;
const num2 = 2;
const num3 = 16
const num4 = 6
// // multiply variables num1 and numb2 whols results will equal "62"
const multiply = num1 * num2;

// // generate a random integer > 0
const random = Math.floor(Math.random() * 10) + 1;

// // set to an equation whose remainder is equal to "4"
const mod = num3 % num4;

// // return "20" as the highest number in the set
const max = Math.max(1, 5, 10, 15, 20);

// Write your code here